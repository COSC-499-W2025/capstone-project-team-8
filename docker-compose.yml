version: "3.8"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    working_dir: /app
    volumes:
      - ./src/frontend:/app
    environment:
      - NODE_ENV=development
    command: npm run dev