export const metadata = {
  title: 'Dashboard',
  description: 'View your project analytics, skills breakdown, quality scores, and recent activity at a glance.',
  openGraph: {
    title: 'Dashboard | Portfolio Analyzer',
    description: 'View your project analytics, skills breakdown, quality scores, and recent activity at a glance.',
  },
};

export default function DashboardLayout({ children }) {
  return children;
}
