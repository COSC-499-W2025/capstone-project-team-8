export const metadata = {
  title: 'Upload Project',
  description: 'Upload a project to analyze its codebase, detect languages and frameworks, and extract skills automatically.',
  openGraph: {
    title: 'Upload Project | Portfolio Analyzer',
    description: 'Upload a project to analyze its codebase, detect languages and frameworks, and extract skills automatically.',
  },
};

export default function UploadLayout({ children }) {
  return children;
}
